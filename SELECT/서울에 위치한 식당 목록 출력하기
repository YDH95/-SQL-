SELECT a.REST_ID, b.REST_NAME, b.FOOD_TYPE, FAVORITES, ADDRESS, 
ROUND(AVG(REVIEW_SCORE), 2) AS SCORE
FROM REST_REVIEW a
LEFT JOIN REST_INFO b ON a.REST_ID = b.REST_ID
WHERE ADDRESS LIKE '서울%'
GROUP BY REST_ID
ORDER BY SCORE DESC, FAVORITES DESC
